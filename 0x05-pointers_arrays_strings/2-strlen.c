#include <stdio.h>
#include <string.h>
/**
 * _strlen - return length of function
 * @s: pointer to string
 * Return: length of string
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
